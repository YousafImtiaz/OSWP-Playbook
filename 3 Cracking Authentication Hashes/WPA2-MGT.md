> The tool used here is asleap we will take the command provided and add our wordlist:

```
asleap -C <challenge_value> -R <response_value> -W /usr/share/john/password.lst
```

We can also use rockyou.txt if needed.